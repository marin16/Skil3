#ifndef UTILITIES_H
#define UTILITIES_H

#include <sstream>

/*
 *  Reusable utility funcitons
 */
namespace utils {
    // sets string to lower (source: example solution for week 1 dabs)
    std::string stringToLower(std::string str);
    // Converts int ot string (source: example solution for week 1 dabs)
    std::string intToString(int number);
}

#endif // UTILITIES_H
